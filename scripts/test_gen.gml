gen_obj(objTree, 1, 0) // generates trees flat across board (slope = 0)
gen_obj(objTree, 3, 1) // generates trees with higher prob at edges (slope = 1)
gen_tree_wall() // generates tree walls on either edge
